{
  "name": "git-hours",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsup",
    "prepare": "npm run build",
    "test": "tsx --test tests/*.test.ts"
  },
  "bin": {
    "git-hours": "./dist/index.js"
  },
  "files": [
    "dist/"
  ],
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsup": "^8.3.5",
    "tsx": "^4.20.0",
    "typescript": "^6.0.3"
  }
}
